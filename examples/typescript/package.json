{
  "name": "esptool-js-backend-example",
  "version": "1.0.0",
  "description": "This an example of using esptool-js with parcel and typescript",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "npm run clean && parcel build src/index.html",
    "clean": "rimraf dist .parcel-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.3",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.4"
  }
}
